var lat = 0;
var long = 0;

var getPokeList = function () {
  return true; //prevents errors while function not defined
  // generate a list of pokemon and display on page
  //for each pokemon in list
  //return pokedex number & name

  //OPTIONAL: paginated list? depending on runtime
};

var choosePokemon = function () {
  if (lat != 0) {
    // clicked pokemon number assigned to lat
  } else if (long != 0) {
    //clicked pokemon number assigned to long
  } else {
    // prompt user to clear and try again
  }
};

var latLong = function () {
  //define what parameters to pass
  return true; //prevents errors while function not defined
};
